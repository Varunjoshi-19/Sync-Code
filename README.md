## FOR PRISMA

1. npx prisma generate // that's it for to sync it with your local codebase.
2. setup the .env DB URL 



### BACKEND SCRIPT FOR PRODUCTION : 

so basically just move all the dev dep into dep that it 
and here the script for production : 


ts-node : directly run the TS file without converting it into JS.
node : for running js files.
NODE_ENV : auto set to production if you are using node server.js
